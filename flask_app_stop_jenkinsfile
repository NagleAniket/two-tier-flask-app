pipeline {
    agent { label 'jenkins_slave' }

    stages {
        stage("Checkout Code") {
            steps {
                checkout scm
            }
        }

        stage("Docker Stop") {
            steps {
                sh 'docker compose -f docker-compose.yml down'
            }
        }
    }
}
